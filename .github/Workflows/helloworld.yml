name: "Hello world workflow"
# this will trigger workflow on psh doe master bbranch
on:
  push:
    branches:
      - master
      - production


jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: "Print Hello World"
        run: echo "hello world"